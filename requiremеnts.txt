Приветствую Вас за чтением пояснительной записки!

Необходимые библиотеки:
import requests
import os
import json
import sys

Файлы фотографий профиля будут сохраняться в папку "NETOLOGY" по умолчанию в проекте на ПК и в облачном хранилище.

Файл "sample.json" по умолчанию будет выведен в корневой каталог проекта.

Логин пользователя диска будет выведен в консоль.

Токены вводятся с консоли, однако, подготовил версию для загрузки ключей из текстового файла при необходимости.

P.S. цели описания всех некорректных вводов и ответов на запросы не ставилась, проработаны лишь некоторые из них.

Спасибо за внимание!